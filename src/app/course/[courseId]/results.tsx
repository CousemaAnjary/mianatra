import { ScreenPlaceholder } from "@/src/components/shared";

export default function CourseResultsScreen() {
  return (
    <ScreenPlaceholder
      title="Résultats du cours"
      description="La coquille de résultats est prête pour recevoir les indicateurs du cours."
    />
  );
}
